version: '3.8'
services:
  app:
    image: ://microsoft.com
    volumes:
      - ..:/workspace:cached
    network_mode: service:db
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: testdb
    ports:
      - "3306:3306"